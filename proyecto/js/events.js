// Gestión de eventos
document.addEventListener('DomContentLoaded',()=>{
    Dom_Equipos(datosEquipos);

    const botonesNav = document.querySelectorAll('#navegador-lateral button'); //busca botones dentro del nav//
    const secciones


});
